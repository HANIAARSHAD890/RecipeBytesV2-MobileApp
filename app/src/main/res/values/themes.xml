<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeBytes" parent="Theme.Material3.DayNight.NoActionBar">
        <item name= "android:fontFamily">@font/poppins_semibold</item>
            <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="App.CustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:height">2dp</item>
        <item name="android:width">20dp</item>
        <!-- This makes the underline white -->
        <item name="colorSecondaryContainer">@color/background</item>
        <item name="android:layout_marginVertical">10dp</item>
    </style>

    <style name="Theme.RecipeBytes" parent="Base.Theme.RecipeBytes" />
</resources>